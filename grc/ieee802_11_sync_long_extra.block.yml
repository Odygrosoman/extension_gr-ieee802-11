# auto-generated by grc.converter

id: ieee802_11_sync_long_extra
label: WiFi Sync Long Extended
category: '[IEEE802.11]'

parameters:
-   id: log
    label: Log
    dtype: bool
    default: 'False'
    options: ['True', 'False']
    option_labels: [Enable, Disable]
-   id: debug
    label: Debug
    dtype: bool
    default: 'False'
    options: ['True', 'False']
    option_labels: [Enable, Disable]
-   id: sync_length
    label: Sync Length
    dtype: int
    default: '240'

inputs:
-   domain: stream
    dtype: complex
    multiplicity: '1'
-   label: in delayed
    domain: stream
    dtype: complex
    multiplicity: '1'

outputs:
-   domain: stream
    dtype: complex
    multiplicity: '1'
asserts:
- ${ sync_length > 0 }

templates:
    imports: import ieee802_11
    make: ieee802_11.sync_long_extra(${sync_length}, ${log}, ${debug})

file_format: 1
